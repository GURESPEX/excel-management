{"_type":"issue","id":"excel-management-liw","title":"Excel-based Employee Management Web App (MVP)","description":"## Problem Statement\n\nThe user has an interview take-home exercise: given an Excel file of employee data (ID, Name, Department, Salary, Join Date, Status, Last Updated Date) and a short requirements doc, they must build a web app that lists, searches/filters, and CRUDs this data, using an AI coding assistant to do it. They also want to differentiate their submission with AI Integration, Import/Export support, and readiness for multiple database backends — while a hard interview deadline (tomorrow, 17:00–18:00) constrains how much of that is actually buildable versus talking points.\n\n## Solution\n\nA .NET 9 ASP.NET Core Web API + Vite/React/TypeScript SPA managing Employee records, backed by SQLite via EF Core (written against EF Core abstractions so the provider is swappable later). Users list, search/filter, and CRUD employees through the UI. Employee data enters the system only through an Excel import matching the sample file's schema (upsert by ID, all-or-nothing validation) or manual entry; a matching Export produces an Excel file of the current data. An MCP server hosted in the same ASP.NET process exposes the same CRUD business logic as MCP tools over Streamable HTTP, so an AI coding assistant can drive the app directly — this is what satisfies the \"AI Integration\" ask, deliberately instead of an in-app AI feature.\n\n## User Stories\n\n1. As an HR administrator, I want to see a list of all employees in a table, so that I can review current headcount data at a glance.\n2. As an HR administrator, I want to search employees by name, so that I can quickly find a specific person.\n3. As an HR administrator, I want to filter employees by Department, so that I can see everyone in a specific team.\n4. As an HR administrator, I want to filter employees by Status (Active/In Active), so that I can distinguish current staff from former staff.\n5. As an HR administrator, I want to add a new employee via a form, so that new hires are recorded in the system.\n6. As an HR administrator, I want the ID field auto-generated when I create an employee, so that I never have to manage unique identifiers manually.\n7. As an HR administrator, I want to pick the Department from a dropdown populated with existing values, so that I don't introduce typos or duplicate department names.\n8. As an HR administrator, I want to add a brand-new Department value if it doesn't exist yet, so that the dropdown doesn't block a legitimate new department.\n9. As an HR administrator, I want Salary displayed and entered with `#,##0.00` formatting, so that figures are easy to read and consistent.\n10. As an HR administrator, I want to pick Join Date from a calendar widget, so that dates are always valid and consistently formatted.\n11. As an HR administrator, I want to toggle Status with a single checkbox (Active/In Active), so that updating employment status is a one-click action.\n12. As an HR administrator, I want Last Updated Date stamped automatically whenever a record changes, so that I always know how fresh the data is without manual entry.\n13. As an HR administrator, I want to edit an existing employee's details, so that I can correct or update their record.\n14. As an HR administrator, I want to delete an employee record, so that I can remove entries that no longer belong in the system.\n15. As an HR administrator, I want to import an Excel file matching the sample schema, so that I can bulk-load or bulk-update employee data instead of entering it one by one.\n16. As an HR administrator, I want import to upsert by ID (update existing, insert new), so that re-importing a corrected file doesn't wipe out manual edits made through the UI.\n17. As an HR administrator, I want the whole import rejected with a clear list of row-level errors if any row is invalid, so that I never end up with a silently incomplete or corrupted dataset.\n18. As an HR administrator, I want to export the current employee data to an Excel file, so that I can share or archive a snapshot outside the app.\n19. As an HR administrator, I want the app to start with an empty dataset (no auto-seeded data), so that only data I explicitly import is ever in the system.\n20. As an AI coding assistant (MCP client), I want to call the same CRUD operations exposed as MCP tools, so that I can list, create, update, and delete employees on the user's behalf during a live demo.\n21. As an AI coding assistant (MCP client), I want to connect via Streamable HTTP at a single well-known endpoint, so that I can be registered with one `claude mcp add --transport http` command with no extra process management.\n22. As a developer extending this app, I want the persistence layer written against EF Core abstractions only, so that swapping SQLite for PostgreSQL or SQL Server later requires no business-logic changes.\n23. As a developer/interviewer, I want the app runnable locally with one command per side (`dotnet run` for the API, `npm run dev` for the frontend), so that it's trivially demoable.\n24. As a developer, I want unit tests directly on the employee business-logic service covering validation, upsert, and status rules, so that business rules are verified in isolation from HTTP/DB wiring.\n25. As a developer, I want integration tests through the real HTTP surface against a real SQLite database, so that the same seam covers both the REST controllers and the MCP tool wrappers that call the same service.\n26. As a developer, I want a Bruno collection checked into the repo covering the core endpoints, so that I can manually exercise or demo the API during the interview without relying only on the UI or automated tests.\n\n## Out of Scope\n\n- Authentication/authorization at any layer (REST API, MCP endpoint, or frontend).\n- Runtime switching between database providers via UI or config toggle — only code-level provider-agnosticism (EF Core abstraction).\n- Natural-language search or chat-based data entry as an in-app feature — superseded by the MCP integration.\n- Importing/merging Excel files with any schema other than the exact sample schema.\n- Partial/best-effort import (skip-invalid-rows) — import is strictly all-or-nothing.\n- Automated frontend tests (component or E2E) — frontend is manually QA'd only, given the deadline.\n- Multi-tenant support or simultaneous multi-database connections.\n","design":"## Implementation Decisions\n\n- Two top-level projects (already scaffolded as empty placeholders): an ASP.NET Core Web API project and a Vite + React + TypeScript SPA project.\n- Domain entity `Employee`: Id, Name, Department, Salary, JoinDate, Status, LastUpdatedDate. Status modeled as a boolean/enum (Active/InActive) at the domain layer, presented as a checkbox in the UI.\n- Persistence: EF Core against SQLite via a single DbContext; all data access written against EF Core abstractions only (no raw/provider-specific SQL), so switching to a PostgreSQL or SQL Server provider later is a configuration-only change (new provider package + connection string), not a business-logic change.\n- No database seeding on startup — the database starts empty. Data enters the system only through Import or manual Create.\n- Business logic centralized in a single application-layer service (e.g. `EmployeeService`) that both the REST endpoints and the MCP tools call — this is the shared seam that both testing decisions and both API surfaces rely on.\n- REST API (Minimal API): list/search/filter, get by id, create, update, delete, import, export.\n- Search/Filter implemented server-side (query parameters translated to EF Core LINQ against the DB), not client-side filtering of a fully loaded dataset.\n- Department dropdown populated dynamically from the distinct Department values already in the data; the UI allows entering a new department value not yet present.\n- Import: accepts an `.xlsx` file matching the exact column schema of the sample Excel file (ID, Name, Department, Salary, Join Date, Status, Last Updated Date), read via ClosedXML. All rows are parsed and validated first; if any row fails validation, the entire import is rejected atomically with the full list of row-level errors returned; if all rows are valid, rows upsert by ID (matching ID updates the record, new ID inserts one).\n- Export: generates an `.xlsx` file (via ClosedXML) with the same column schema, containing the full current dataset, returned as a file download.\n- MCP integration: hosted via the official ModelContextProtocol C# SDK (ASP.NET Core hosting package) in the same process as the Web API, exposed over Streamable HTTP at a single endpoint. MCP tools are thin wrappers around the same `EmployeeService` methods the REST endpoints call — full CRUD is exposed (list/search, get, create, update, delete), not read-only.\n- No authentication/authorization anywhere in the app.\n- Frontend: single-page layout, no router — the employee table is the whole app, with Create/Edit opening in a Dialog over it rather than a separate route. Delete uses the browser's native `confirm()` rather than a custom confirmation dialog.\n- Frontend styling: Tailwind CSS + shadcn/ui (Radix UI primitives under the hood, components scaffolded into the repo via the shadcn CLI rather than installed as an opaque package) for Button, Input, Select, Checkbox, Table, Dialog, Badge (Status), and Alert (import error list). Icons via lucide-react (ships with shadcn).\n- Join Date stays a native `\u003cinput type=\"date\"\u003e` (styled via shadcn's Input), not a shadcn Popover+Calendar date picker — the native control already satisfies the \"Calendar Box Input\" requirement without the extra composition.\n- Create/Edit form built with react-hook-form + Zod (validation schema) + @hookform/resolvers, using shadcn's Form component — the Zod schema is the single source of truth for both the inferred TypeScript type and the validation rules (required Name, non-negative Salary, valid JoinDate, non-empty Department), instead of hand-written controlled-input validation.\n- Data fetching: plain `fetch` calls to the REST API — no TanStack Query or other data-fetching library; the app's scope (one entity, a handful of endpoints) doesn't justify one.\n- \"Multi-database support\" is a code-level property of the EF Core abstraction, not a runtime feature — no in-app UI/config to switch providers live; demonstrated by pointing at the DbContext configuration, not by standing up multiple live databases.\n- In-app AI features (NL search, chat-based entry) are explicitly deferred — the MCP server is the entire \"AI Integration\" deliverable.\n\n## Testing Decisions\n\n- Two automated test seams:\n  1. **Unit tests on the `EmployeeService`** (xUnit) — covering validation rules (bad date, negative salary, invalid Status value), the upsert-by-ID logic, and Department-dropdown value derivation. These assert business-rule behavior only, isolated from HTTP and EF Core wiring where practical.\n  2. **Integration tests via `WebApplicationFactory\u003cProgram\u003e`** (xUnit) — exercising the real HTTP endpoints (list/search/filter, CRUD, import, export) against a real temp-file SQLite database. This is the same seam that covers the MCP tool wrappers, since they call the same `EmployeeService`.\n- A checked-in Bruno collection covering the core endpoints (list, search, create, update, delete, import, export), for manual/scripted verification and live-demo use during the interview. Not part of the automated suite — a supplementary tool the user already has (Bruno CLI) available.\n- A good test asserts observable behavior (HTTP response status/body, or service return values/thrown exceptions) — never internal implementation details such as which private method was called.\n- No prior art in this repo yet (greenfield project) — these two seams establish the testing pattern for all future work here.\n\n## Further Notes\n\n- Hard deadline: demo-ready by 2026-09-14, 17:00–18:00 (interview slot). If time runs out, priority order is: (1) core CRUD + search/filter + Import/Export, (2) MCP server, (3) polish/styling. The unit-test seam and multi-DB code-level abstraction are cheap under this architecture and should not be the first things cut.\n- The interviewer has reserved a \"Live Challenge\" segment for an on-the-spot feature addition using AI tools — the MCP server doubles as the rehearsed answer to \"how would you let an AI agent drive this app,\" and is worth demoing live if time allows.\n- `excel-management-api` and `excel-management-web` already exist as empty placeholder directories in the repo — this spec assumes they are the target locations for the two projects.\n","notes":"Interview demo deadline: 2026-09-14 17:00-18:00. Priority if time runs short: CRUD+Search/Filter+Import/Export \u003e MCP server \u003e styling polish.","status":"open","priority":1,"issue_type":"feature","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:25:27Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:40:30Z","labels":["ready-for-agent"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"excel-management-liw.8","title":"Test seams + Bruno collection","description":"The two automated test seams (unit tests directly on EmployeeService covering validation/upsert/derivation rules; integration tests via WebApplicationFactory\u003cProgram\u003e exercising the real HTTP endpoints against a real temp-file SQLite database — the same seam that covers the MCP tool wrappers) plus a checked-in Bruno collection covering list/search/create/update/delete/import/export for manual/live-demo verification.","acceptance_criteria":"- [ ] xUnit unit test project covers EmployeeService validation rules, upsert-by-ID logic, and Department-dropdown derivation\n- [ ] xUnit integration test project spins up WebApplicationFactory\u003cProgram\u003e against a real temp SQLite DB and exercises list/search/filter, CRUD, import, export via HTTP\n- [ ] Both test projects run via a single command (e.g. dotnet test) and pass\n- [ ] A Bruno collection is checked into the repo with requests for list, search, create, update, delete, import, export","status":"open","priority":2,"issue_type":"task","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:51:11Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:51:11Z","labels":["ready-for-agent"],"dependencies":[{"issue_id":"excel-management-liw.8","depends_on_id":"excel-management-liw","type":"parent-child","created_at":"2026-09-13T20:51:11Z","created_by":"Natthanan Thongchomphu","metadata":"{}"},{"issue_id":"excel-management-liw.8","depends_on_id":"excel-management-liw.7","type":"blocks","created_at":"2026-09-13T20:51:11Z","created_by":"Natthanan Thongchomphu","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"excel-management-liw.7","title":"MCP server","description":"An MCP server hosted in the same ASP.NET Core process (official ModelContextProtocol C# SDK, ASP.NET Core hosting package), exposed over Streamable HTTP at a single endpoint, with tools wrapping EmployeeService's list/search, get, create, update, and delete methods — the same business logic the REST endpoints call, giving an MCP client full CRUD.","acceptance_criteria":"- [ ] MCP endpoint is reachable over Streamable HTTP at a single well-known path\n- [ ] MCP tools exist for list/search, get, create, update, delete, each calling EmployeeService directly\n- [ ] Registering the server via 'claude mcp add --transport http \u003curl\u003e' and calling a tool (e.g. list) returns real data from the same database the REST API/UI use","status":"open","priority":2,"issue_type":"task","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:51:09Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:51:09Z","labels":["ready-for-agent"],"dependencies":[{"issue_id":"excel-management-liw.7","depends_on_id":"excel-management-liw","type":"parent-child","created_at":"2026-09-13T20:51:09Z","created_by":"Natthanan Thongchomphu","metadata":"{}"},{"issue_id":"excel-management-liw.7","depends_on_id":"excel-management-liw.4","type":"blocks","created_at":"2026-09-13T20:51:10Z","created_by":"Natthanan Thongchomphu","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"excel-management-liw.6","title":"Export to Excel","description":"An 'Export' action generating an .xlsx file (via ClosedXML) containing the full current dataset in the same column schema as the sample file, returned as a file download.","acceptance_criteria":"- [ ] GET export endpoint returns an .xlsx file with all current employees, columns matching the sample schema\n- [ ] Frontend 'Export' button triggers the download","status":"open","priority":2,"issue_type":"task","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:51:08Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:51:08Z","labels":["ready-for-agent"],"dependencies":[{"issue_id":"excel-management-liw.6","depends_on_id":"excel-management-liw.2","type":"blocks","created_at":"2026-09-13T20:51:08Z","created_by":"Natthanan Thongchomphu","metadata":"{}"},{"issue_id":"excel-management-liw.6","depends_on_id":"excel-management-liw","type":"parent-child","created_at":"2026-09-13T20:51:07Z","created_by":"Natthanan Thongchomphu","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"excel-management-liw.5","title":"Import from Excel","description":"An 'Import' action accepting an .xlsx file matching the sample schema exactly, read via ClosedXML; all rows validated first (same rules as Create/Edit) — if any row is invalid, nothing is written and the full list of row-level errors is shown; if all rows are valid, each row upserts by ID against EmployeeService (existing ID updates, new ID inserts).","acceptance_criteria":"- [ ] POST import endpoint parses the uploaded file's rows via ClosedXML\n- [ ] All rows are validated before any write; any invalid row aborts the whole import with zero writes\n- [ ] Row-level errors (row number + reason) are returned and rendered (shadcn Alert) on failure\n- [ ] Valid rows upsert by ID: existing ID updates the record, new ID inserts one\n- [ ] Table reflects the imported data after a successful import","status":"open","priority":2,"issue_type":"task","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:51:06Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:51:06Z","labels":["ready-for-agent"],"dependencies":[{"issue_id":"excel-management-liw.5","depends_on_id":"excel-management-liw.4","type":"blocks","created_at":"2026-09-13T20:51:06Z","created_by":"Natthanan Thongchomphu","metadata":"{}"},{"issue_id":"excel-management-liw.5","depends_on_id":"excel-management-liw","type":"parent-child","created_at":"2026-09-13T20:51:05Z","created_by":"Natthanan Thongchomphu","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"excel-management-liw.4","title":"Edit \u0026 Delete employee","description":"Row-level 'Edit' (opens the same Dialog/form pre-filled, submitting to a REST update endpoint) and 'Delete' (browser confirm(), then a REST delete endpoint) actions on the employee table.","acceptance_criteria":"- [ ] PUT/PATCH endpoint updates an employee via EmployeeService, re-running the same validation as create, and re-stamps LastUpdatedDate\n- [ ] DELETE endpoint removes an employee via EmployeeService\n- [ ] Edit form pre-fills existing values; Delete asks for confirmation via window.confirm() before calling the endpoint\n- [ ] Table reflects edits/deletes without a page reload","status":"open","priority":2,"issue_type":"task","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:51:04Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:51:04Z","labels":["ready-for-agent"],"dependencies":[{"issue_id":"excel-management-liw.4","depends_on_id":"excel-management-liw.3","type":"blocks","created_at":"2026-09-13T20:51:04Z","created_by":"Natthanan Thongchomphu","metadata":"{}"},{"issue_id":"excel-management-liw.4","depends_on_id":"excel-management-liw","type":"parent-child","created_at":"2026-09-13T20:51:03Z","created_by":"Natthanan Thongchomphu","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"excel-management-liw.3","title":"Create employee","description":"A 'New Employee' action opening a shadcn Dialog with a react-hook-form + Zod form (Name, Department [dropdown of existing values + free entry], Salary, JoinDate, Status checkbox), submitting to a REST create endpoint that validates and returns the created employee (auto-generated Id, stamped LastUpdatedDate) into the table.","acceptance_criteria":"- [ ] POST endpoint creates an employee via EmployeeService, auto-generating Id and stamping LastUpdatedDate\n- [ ] EmployeeService rejects invalid input (empty Name, negative Salary, invalid JoinDate, empty Department) with field-level errors\n- [ ] Department dropdown is populated from distinct existing values and accepts a new value\n- [ ] Salary input/display uses #,##0.00 formatting\n- [ ] New employee appears in the table without a page reload","status":"open","priority":2,"issue_type":"task","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:51:02Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:51:02Z","labels":["ready-for-agent"],"dependencies":[{"issue_id":"excel-management-liw.3","depends_on_id":"excel-management-liw","type":"parent-child","created_at":"2026-09-13T20:51:02Z","created_by":"Natthanan Thongchomphu","metadata":"{}"},{"issue_id":"excel-management-liw.3","depends_on_id":"excel-management-liw.2","type":"blocks","created_at":"2026-09-13T20:51:02Z","created_by":"Natthanan Thongchomphu","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"excel-management-liw.2","title":"Listing + Search/Filter","description":"A REST endpoint that lists employees with server-side search (by Name) and filter (by Department, by Status), plus a frontend table (shadcn/ui Table) rendering the list, with a search input and Department/Status filter controls that re-query the server as the user changes them.","acceptance_criteria":"- [ ] GET endpoint accepts query params for name search, department filter, status filter, and returns matching employees\n- [ ] Search/filter logic lives in EmployeeService as LINQ against the DbContext, not client-side filtering of a fully loaded dataset\n- [ ] Frontend table lists all employees with columns for all 7 fields\n- [ ] Frontend has a search input and Department/Status filter controls that update the table via the REST endpoint","status":"open","priority":2,"issue_type":"task","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:50:35Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:50:35Z","labels":["ready-for-agent"],"dependencies":[{"issue_id":"excel-management-liw.2","depends_on_id":"excel-management-liw","type":"parent-child","created_at":"2026-09-13T20:50:35Z","created_by":"Natthanan Thongchomphu","metadata":"{}"},{"issue_id":"excel-management-liw.2","depends_on_id":"excel-management-liw.1","type":"blocks","created_at":"2026-09-13T20:51:00Z","created_by":"Natthanan Thongchomphu","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"excel-management-liw.1","title":"Scaffold + Employee seam","description":"A running (but behaviorally empty) ASP.NET Core Web API and Vite/React/TypeScript frontend, wired to a SQLite database via EF Core through a single DbContext, with an Employee entity (Id, Name, Department, Salary, JoinDate, Status, LastUpdatedDate) and an EmployeeService seam (list/get/create/update/delete methods) that pass straight through to EF Core — no validation or business rules yet. This is the shared seam every later ticket in this epic builds behaviour behind.","acceptance_criteria":"- [ ] `dotnet run` starts the API and it responds on a health/root endpoint\n- [ ] `npm run dev` starts the frontend and it loads against the API with no manual setup beyond `dotnet restore`/`npm install`\n- [ ] SQLite database is created via EF Core with an Employee table matching the 7 sample-schema columns\n- [ ] EmployeeService exists with list/get/create/update/delete methods\n- [ ] No seed data — the database starts empty","status":"open","priority":2,"issue_type":"task","owner":"natthanan.th@techconsbiz.com","created_at":"2026-09-13T13:50:23Z","created_by":"Natthanan Thongchomphu","updated_at":"2026-09-13T13:50:23Z","labels":["ready-for-agent"],"dependencies":[{"issue_id":"excel-management-liw.1","depends_on_id":"excel-management-liw","type":"parent-child","created_at":"2026-09-13T20:50:22Z","created_by":"Natthanan Thongchomphu","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
